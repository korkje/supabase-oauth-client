# Changelog

## 0.1.0 (unreleased)

- Initial release: PKCE authorize URL builder, out-of-band `exchange()`, same-device `completeFromUrl()`,
  refresh with rotation, background refresh timer with backoff, coalesced `getAccessToken()`,
  memory / Web Storage / async storage adapters, public and confidential client authentication,
  typed `OAuthError`. Verified against a live Supabase project (see README).
